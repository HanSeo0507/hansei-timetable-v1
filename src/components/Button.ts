import styled from "styled-components";

const Button = styled.button`
	border: none;

	color: white;
	font-size: 16px;
	font-weight: 700;
	background-color: rgba(4, 173, 234, 1);

	border-radius: 0.5rem;
	padding: 0.8rem 1rem;
`;

export default Button;
